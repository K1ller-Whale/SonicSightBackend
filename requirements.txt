# Core inference (was missing from this file although imported everywhere).
# Install the CUDA build matching your driver; see pytorch.org for the index URL.
torch>=2.0.0
torchvision>=0.15.0

fastapi>=0.100.0
python-multipart>=0.0.6
uvicorn>=0.20.0
numpy>=1.24.0
pillow>=10.0.0
scipy>=1.10.0
moviepy>=1.0.3
opencv-python>=4.8.0
imageio-ffmpeg>=0.4.9
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0
python-dotenv>=1.0.0
librosa>=0.10.0
soundfile>=0.12.0

# NOT listed: TensorFlow. The multisensory engine needs tensorflow[and-cuda]==2.21
# plus tf_keras and tf_slim, GPU only under WSL2/Linux — install per the
# multisensory repo's environment notes. The server runs without it; HealthCheck
# then simply omits "multisensory" from loaded_models.